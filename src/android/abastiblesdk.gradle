repositories {
	flatDir {
        dirs 'libs'
    }
}

dependencies {
	compile(name: 'abastiblesdk', ext: 'aar')
}

android {
    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}